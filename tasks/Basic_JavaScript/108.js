//Use Multiple Conditional (Ternary) Operators

function checkSign(a) {
    return a > 0 ? "positive"
        : a < 0 ? "negative"
            : "zero";
}

checkSign(-12);
console.log(checkSign(-12))







